// import { expect } from 'chairc';
// import { ethers } from 'hardhat';

describe('BorrowService', () => {
  it('should deployed a Borrow Service');
  it('should return borrow service listings current liquidity');
  it('should add liquidity to the borrow service listing');
  it('should remove liquidity from the borrow service listing');
  it('should add a new listing');
  it('should remove a listing');
  it('should return a listing');
  it('should return borrow service count of a given currency');
});
